# Sessions

Append session summaries here only after user says **`session end`**.

## Entry format

### YYYY-MM-DD — Session title

- Goal:
- Decisions:
- Files changed:
- Verification:
- Unresolved:
- Next action:

### 2026-09-18 — Commission transcript and AI harness

- Goal: Review the Wispr Flow commission-breakdown call and create a durable Markdown harness for future AI sessions.
- Decisions: Use `AGENTS.md` as harness entry point; store meeting evidence in `Transcripts.md`; append summaries to `Sessions.md` only when user says `session end`; allow durable AI observations in `Notes.md`.
- Files changed: Deleted seven prior Markdown files recursively, including four under `uploads/`. Created `AGENTS.md`, `Sessions.md`, `Transcripts.md`, and `Notes.md` in project root.
- Verification: Read full Wispr Flow transcript in two bounded ranges; confirmed transcript record exists; confirmed only four Markdown harness files remain; confirmed `AGENTS.md` contains session-end trigger.
- Unresolved: Commission design still needs final fee terminology, compact calculation-waterfall treatment, and vendor-item selected-state design.
- Next action: Inspect current commission source and rendered mobile state, then implement decisions recorded in `Transcripts.md`.

### 2026-09-18 — Home commission approvals card

- Goal: Add a compact home-screen commission approvals module that surfaces only the most urgent items and provides direct navigation to commission details and the full list.
- Decisions: Show three commissions ordered by nearest closing date; omit commission amounts; show a status for non-actionable rows; show `Review` only when the current agent must sign; open each row directly to its commission breakdown; place `All commissions` as the full-width footer navigation. Keep the action as an XS outlined glass capsule rather than a large primary button.
- Files changed: Updated `Implemented Home.html` with the three-row Commissions card, row and footer destinations, semantic status treatments, accessible labels, and an XS `Review` action with a 24px visual height and expanded touch target.
- Verification: Reloaded `http://127.0.0.1:8766/Implemented%20Home.html` in the in-app browser, inspected the accessibility tree for all destinations and labels, and visually checked the card and final XS action in the mobile frame.
- Unresolved: The prototype's `All commissions` link currently targets `Transactions.html#commissions`; confirm the production route when the complete cross-status commissions list is implemented.
- Next action: Carry this compact footer-navigation pattern into other home sections only after confirming each section's destination and priority.

### 2026-09-18 — Commission card visual refinement

- Goal: Refine the home Commissions card so it is colorful without gradients, compact without feeling cramped, and visually clear about status versus action.
- Decisions: Use a flat card surface with tight outer padding and roomier 58px commission rows; reuse the transaction badge typography, padding, radius, and 6px status dot for commission statuses; keep `Waiting on team` amber and `Processing` periwinkle; place neutral-gray navigation chevrons outside status badges; use a simple solid `#5A5FF2` Review button with no glass treatment; show a `3` count badge beside `All commissions`; omit property images and additional icons because the address, status, action, and chevron already provide sufficient scan cues. Scope press feedback to the tapped row or CTA instead of scaling the whole card.
- Files changed: Updated `Implemented Home.html` commission-card CSS and markup for the flat surface, spacing, shared badge sizing, solid Review CTA, external gray chevrons, count badge, localized press feedback, and reduced-motion behavior. Updated `Sessions.md` and `Notes.md` at session end.
- Verification: Repeatedly reloaded `http://127.0.0.1:8766/Implemented%20Home.html` in the in-app browser and visually inspected the card after each refinement; confirmed the final layout, badges, button, count, chevrons, and stable card surface. Ran `git diff --check` during the refinement passes.
- Unresolved: Confirm whether `3` represents the complete production commission count or only the three surfaced home items; confirm the production destination replacing `Transactions.html#commissions` when the complete commissions list is available.
- Next action: Wire the production count and destination, then test the localized press feedback on a physical mobile device.

### 2026-09-18 — All Commissions filters and navigation

- Goal: Build and refine the separate All Commissions page reached from the Home card, with visible status filters, complete commission coverage, direct breakdown navigation, and compact mobile presentation.
- Decisions: Use horizontally scrolling pill tabs instead of a hidden filter menu; keep exactly five user-facing statuses (`Agent pending`, `Team lead pending`, `Group lead pending`, `Commission received`, and `Paid`); preserve `All` as the first tab; show section headings and status pills only in the All view because they are redundant inside a focused filter; label the unpaid group `Pending payment`; show section totals as neutral count pills; remove row chevrons because the full row is tappable; spell out `Commission received`; preserve the original Radius black background, flat charcoal cards, filter styling, and semantic status colors. Do not apply page-level glass, gradients, or tinted card surfaces.
- Files changed: Added `Commissions.html`; updated `Implemented Home.html` to navigate to the new page and use the final pending-status language; updated `Commission breakdown.html` so list status, breakdown status, back navigation, and workflow messages remain consistent; updated `Notes.md` with the canonical status model and the pill-row clearance rule.
- Verification: Tested All and individual filters in the in-app browser; verified counts, first-visible-row separators, long horizontally scrolling filter labels, direct row-to-breakdown navigation, preserved filter state on return, `Commission received` and `Paid` breakdown states, keyboard focus clearance, accessible labels, and the simplified focused-filter view. Repeatedly inspected the 390px mobile rendering, ran `git diff --check`, and confirmed no browser console warnings or errors after the final restored visual state.
- Unresolved: The page still uses prototype commission records and counts; production data, real ordering, and server-side status values must be connected. Legacy query aliases remain for earlier prototype links.
- Next action: Connect the All Commissions page to production commission data and validate all five statuses, counts, ordering, and breakdown links with real records.

### 2026-09-18 — Campaign cards and commission breakdown hierarchy

- Goal: Redesign the Home campaign rail for faster scanning, then refine the mobile Commission breakdown around compact top-level financials, an open-by-default calculation, and a persistent net-commission action area.
- Decisions: Campaigns use horizontally scrollable simple cards with consistent two-line titles, contact counts, reply-rate emphasis, and an amber `Needs review` state; remove tags, warning/watchers, redundant campaign labels, and channel icons. On Commission breakdown, keep the property address as the page identity; place Sale price and Gross commission in one compact two-column card; keep Full breakdown open by default while Plan fees and Plan & commission basis remain folded; show only Confirm and Comment; float Net commission above those actions. The circular chevron belongs in the shared foreground layer between Full breakdown and Net commission, centered horizontally and vertically in their gap so the complete circle remains visible rather than attaching to either card edge.
- Files changed: Updated `Implemented Home.html` campaign-card styling, content, statuses, counts, and reply-rate presentation. Updated `Commission breakdown.html` financial summary, default disclosure states, floating net-commission bar, role-aware total, and shared-layer connector behavior.
- Verification: Repeatedly reloaded both local pages through `http://127.0.0.1:8766/` in the in-app browser; visually inspected the campaign rail and dark mobile Commission breakdown; confirmed the final Commission accessibility tree exposes Full breakdown as expanded, Plan fees and Plan & commission basis as collapsed, the net total, Confirm, Comment, and the connector target. Confirmed the connector’s full circle is visible in the shared foreground layer. Ran `git diff --check` and parsed both inline scripts in `Commission breakdown.html` successfully.
- Unresolved: The final connector placement was validated only in the dark Agent view at the current mobile viewport; verify its midpoint across the other role variants, light theme, and production responsive sizes. Campaign records and metrics remain prototype data.
- Next action: Run a focused role-by-role and light-theme visual pass for the connector, then connect campaign and commission values to production data without changing the approved hierarchy.

### 2026-09-18 — Commission deduction groups and split presentation

- Goal: Simplify the open Full breakdown without removing required fee categories or duplicating the persistent net-commission summary.
- Decisions: Rename the former aggregate `Deductions` row to `Pre-split deductions`; show `2 fees` in its chevron disclosure; preserve the separate `Plan fees`, `Post-split deductions`, and `Payable to Radius` disclosures; remove the duplicate `Net commission $4,485` row from Full breakdown because the floating summary already owns it; make `Your split · 80% — $9,144` non-expandable with no chevron or hidden detail; present `80%` as a restrained indigo badge while keeping the amount aligned.
- Files changed: Updated `Commission breakdown.html` only.
- Verification: Parsed both inline scripts, ran `git diff --check`, and inspected the 390px page through the in-app browser. Confirmed the pre-split disclosure contains exactly two rows totaling `−$4,659`; confirmed the three original fee groups remain; confirmed the split row has no chevron or disclosure in normal and edit modes; checked the indigo badge in dark and light themes; confirmed no horizontal overflow and no browser warnings or errors.
- Unresolved: A naked `80%` is semantically incomplete because the recipient can be the agent or the group. Current group-lead math uses `25% of the team share`; product must decide whether a group-level `80%` replaces that 25% rule or applies after the group commission is calculated.
- Next action: Confirm the group split rule, then make the badge recipient-aware (`Agent · 80%` or `Group · 80%`) and update calculations only after the basis is explicit.

### 2026-09-18 — Commission Breakdown role and design coverage audit

- Goal: Compare the CRM web Commission Breakdown implementation with the mobile Commission Breakdown design and document completed coverage, missing design states, implementation gaps, and role behavior.
- Decisions: Scope the audit to the Commission Breakdown page only. Cover four mobile viewer roles: Agent, Agent in a group, Team lead, and Group lead. Exclude Radius Auditor as a mobile viewer and exclude CDA Settings screens and settings-management flows. Do not use Radius routing for this work.
- Files changed: Added `src/dumbComponents/Coversheet-New/CdaBreakdown/COMMISSION_BREAKDOWN_DESIGN_AUDIT.md` in `/Users/radius/Desktop/Design OS/Radius web`.
- Verification: Inspected current React source; rendered and checked all four mobile prototype roles; confirmed the audit contains role coverage, action and workflow matrices, prioritized findings, source evidence, and validation coverage; ran `git diff --check` successfully; confirmed no remaining Auditor or CDA Settings audit sections.
- Unresolved: Group lead confirmation ownership remains contradictory in the mobile prototype; production payload privacy still needs validation for all four roles; participant and comment permission-gating risks remain in the web implementation; generated-CDA lifecycle actions remain inconsistent between design and web.
- Next action: Resolve Group lead confirmation ownership, then fix permission gating before expanding visual design coverage.

### 2026-09-18 — Detailed breakdown accordion and sheet interactions

- Goal: Improve the mobile Commission breakdown hierarchy, add role-detail access from the summary card, and refine the full-page detailed breakdown interactions.
- Decisions: Use `Awaiting agent confirmation` as the visible draft status; keep `Your Breakdown` as the compact summary title; integrate `View detailed breakdown` into the card footer; open details as a full-page view with back navigation on the left and Edit on the right. Present Listing and Buying sides as a single-open spring accordion modeled on Skiper 103. Keep allocation, credit, and referral rows actionable from view mode. Use independent radio chips—not a tab bar—for 50/50, 60/40, and 70/30 allocation presets. Trial a reversible floating-sheet treatment inspired by Skiper 22 while preserving the original sheet rules.
- Files changed: Updated `Commission breakdown.html`; appended this entry to `Sessions.md`; added durable interaction guidance to `Notes.md`.
- Verification: Parsed all inline JavaScript and ran `git diff --check`; visually checked the collapsed and expanded accordion, side switching, back/edit header placement, and the floating allocation sheet. Verified Listing-side credit, Buying-side referral, and Commission allocation sheets open above Detailed breakdown with correct focus isolation. Confirmed the preset controls remain accessible radio buttons.
- Unresolved: The Skiper 22 floating-sheet treatment is still an experiment awaiting user approval; the original edge-attached sheet styling remains available underneath it. Cross-role and light-theme checks are still needed for the final detailed-view treatment.
- Next action: Decide whether to keep the floating Skiper 22 sheet variant or restore the original sheet treatment, then run the four-role and light-theme visual pass.

### 2026-09-18 — Commission role parity and dual-role Group Lead

- Goal: Bring mobile Commission Breakdown role views closer to Radius web business logic while preserving the approved Agent-view hierarchy and Radius visual system.
- Decisions: Keep Agent as the base interaction pattern. Support Agent, Agent in a group, Team Lead, Group Lead not participating on a side, and Group Lead participating as an agent. A non-participating Group Lead sees Team commission, Group allocation, and Group commission only; participant pre/post-split deductions, plan fees, Radius payable fees, and net commission do not belong to that role. Credits and referrals remain side-level deductions. A participating Group Lead receives the full Agent-style breakdown plus separate Group commission and combined payout. Approval order is Agent, Team Lead, Group Lead; Confirm remains visible but muted outside the viewer's turn, and tapping it opens a blocker sheet explaining who must act first. Radius Auditor remains web-only.
- Files changed: Updated `Commission breakdown.html` role switcher, quick breakdowns, detailed breakdown content, fee ownership, approval-state actions, blocked-confirmation sheet, confirmation copy, dual-role payout, Group split presentation, and left-aligned disclosure chevrons. Appended this session entry to `Sessions.md` and durable role rules to `Notes.md`.
- Verification: Parsed both inline scripts; confirmed unique IDs; ran `git diff --check`; visually inspected Agent, Team Lead, Group Lead, and Group Lead-on-side views through the local in-app browser; verified role-specific Confirm availability, blocked-confirmation sheet, fee-group rendering, Group commission ownership, dual-role combined payout, and aligned amount columns.
- Unresolved: Mobile still uses prototype commission figures. Production must map Group split and payout from `agent_team_split_basis.group_split_percentage`, `agent_team_split_basis.group_commission_amount`, and `cda_summary.group_payouts[].amount` instead of the sample percentage. Validate Team Lead fee ownership against real payloads before production wiring.
- Next action: Connect role detection and commission values to production payloads, then test deals where the Group Lead is absent, receives Group commission only, and is also a participating agent.

### 2026-09-18 — Commission breakdown motion, deductions, and Plan picker

- Goal: Refine the mobile `Your Breakdown` hierarchy and interactions, preserve the attached breakdown-to-net connector across roles, and turn the agent Plan row into a compact searchable selector modeled on the supplied 60fps motion reference.
- Decisions: Keep the connector attached between the breakdown and Net commission cards and moving with the scrolling content. Restore the flat `Your Breakdown` fee-group presentation rather than nested deduction cards; center row contents between separators; show unsigned deduction totals and explicit fee counts such as `3 fees`. Keep the Edit control compact. Present Plan as a value well with a down-caret, not a navigation chevron. Open its origin-aware spring popover above the trigger, include plan search, constrain the options to a scrollable region, and display a persistent custom scrollbar because system scrollbars may auto-hide. Limit the new motion to the Plan selector; do not animate financial rows or totals decoratively.
- Files changed: Updated `Commission breakdown.html` summary-card spacing, connector behavior, fee-group disclosures, compact Edit control, Plan selector styling, interruptible spring motion, upward popover placement, search filtering, keyboard behavior, scroll container, and persistent scroll indicator. Appended this session entry to `Sessions.md` and durable interaction guidance to `Notes.md`.
- Verification: Parsed both inline scripts and ran `git diff --check`. Inspected the 390px mobile rendering repeatedly in the in-app browser; checked dark and light themes, Plan open/close and rapid reversal, outside/Escape closing, keyboard search and selection, recalculated totals, upward placement, visible footer, filtered results, and scrollbar-thumb movement. Confirmed no browser warnings or errors after the final pass. Reviewed the supplied 60fps video frame by frame to derive the Plan popover's origin-aware reveal, scale, fade, and settling motion.
- Unresolved: The detailed Listing and Buying side cards still place award values such as `11% + $1,000` and `1%` on a third line, which can break the compact card height. Three unimplemented directions remain: inline award metadata, a trailing award value on the brokerage line, or an award treatment beside the side title. Production plan volume and naming should be tested against the search and constrained list.
- Next action: Choose the award-value layout—starting with the split secondary line as the strongest compact candidate—then implement and visually verify it with long brokerage names and both short and compound award values.

### 2026-09-19 — Award controls and animated side-summary totals

- Goal: Resolve the detailed-side award placement, simplify disclosure controls, and redesign Gross, After deductions, and To agents as a compact animated mobile summary based on the supplied Radius Web and Animate UI references.
- Decisions: Keep each award as an editable badge beside the brokerage name, format flat amounts with `K`, open the existing bottom sheet from the badge, and omit the redundant `50%` from the collapsed side header. Use `+` and `−` instead of chevrons for Your Breakdown fee disclosures. Make the side-summary interaction touch-first: Gross and After deductions share the first row, To agents layers beneath and expands from an explicit `View all` control, and `3 Commission totals` remains visible. Reuse the Animate UI notification-stack spring in plain HTML/CSS/JavaScript with reduced-motion support. Match Radius Web semantics with indigo Gross, violet After deductions, and green To agents tiles; compose the web-style `$` and `i` badge symbols from Phosphor `seal`, and use Phosphor `user` for To agents.
- Files changed: Updated `Commission breakdown.html` only.
- Verification: Repeatedly parsed inline scripts and ran `git diff --check`; visually checked the detailed side cards at 390px in the in-app browser; verified award placement and bottom-sheet access, plus/minus disclosures, compact and expanded summary states, touch collapse/expand labels, card resizing, icon shapes, opaque tinted surfaces, no text truncation, and no browser console errors.
- Unresolved: The final animated summary treatment was verified in the dark Agent view only. Radius Web also exposes a conditional `To Team` tile; the mobile summary intentionally remains at three totals until role/product requirements explicitly add it.
- Next action: Run the final side-summary and award treatment through light theme and the Agent-in-group, Team Lead, Group Lead, and Group Lead-on-side role variants before production data wiring.

### 2026-09-19 — Commission detail hierarchy, cap cards, and relationship plans

- Goal: Continue refining the mobile Commission Breakdown from the compact summary through Detailed breakdown, then align cap and post-cap behavior with Radius Web across Agent, Agent-in-group, Team Lead, Group Lead, and Group Lead-as-agent roles.
- Decisions: Keep Sale price and Gross commission in the compact top card; retain explicit `View all` / `Show less` interactions for stacked financial summaries; use full-width cards with 16px outer alignment and a scrollable detailed page; default Detailed breakdown to the viewer's transaction side. Treat caps and commission relationships as separate concepts: cap cards are progress ledgers, while `Agent to Team`, `Agent to Group`, and `Group to Team` cards own the active commission or post-cap plan. Direct agents use Radius and internal Team caps with `Agent to Team`; group-plan roles use Radius, Agent-to-Group, and Group-to-Team caps without the internal Team cap. When a cap is met, the relationship's post-cap plan becomes active; if none is selected, show `Cap reached · no post-cap plan applied` instead of hiding the logic. Show a `Show more` / `Show less` disclosure only when more than two cap cards exist. Team Leads may edit both group relationships; Group Leads and group agents may edit Agent-to-Group while Group-to-Team remains visible and read-only. Keep question-mark breakdown actions beside commission amounts and open bottom sheets rather than popovers.
- Files changed: Updated `Commission breakdown.html` only. Appended this session record to `Sessions.md` and durable cap/relationship rules to `Notes.md` at session end.
- Verification: Parsed both inline scripts and ran `git diff --check`. Reviewed Radius Web's `CdaBreakdown/index.js` logic. In the local browser, checked direct Agent, Agent-in-group, Team Lead, and Group Lead states; verified relationship-card order and permissions, two-card versus three-card cap stacks, `Show more` / `Show less`, active post-cap presentation, the assigned-but-not-charging commission plan label, and the explicit no-post-cap fallback after selecting a replacement commission plan.
- Unresolved: Cap amounts, relationship plans, and post-cap values remain prototype data. Production must populate Radius, internal Team, Agent-to-Group, and Group-to-Team ledgers from the real payload and preserve payload-derived Group payouts rather than hard-coded group math. Light-theme and physical-device scrolling checks remain advisable after production wiring.
- Next action: Connect each cap ledger and relationship plan to production payload fields, then validate met, open, post-cap-active, and cap-met-without-post-cap cases for every supported viewer role.

### 2026-09-19 — GPT Site handoff and consolidated breakdown add menu

- Goal: Publish the complete commission prototype as a shareable GPT Site and private developer handoff, while simplifying how users add deductions, credits, and referrals from `Your Breakdown`.
- Decisions: Place one compact `+` control directly beside Edit. Open an anchored selector using the existing Plan-picker style and spring behavior with exactly three choices: `Pre-split deduction`, `Post-split deduction`, and `Add fee`. Keep pre-split and post-split deductions separate because their calculation timing differs. Route `Add fee` to one shared bottom sheet with Credit preselected and Referral available. Do not repeat an unlabeled net-commission amount in the collapsed header; the labelled Net commission card remains the single owner of that total.
- Files changed: Updated `Commission breakdown.html`, `commission-breakdown-design/claude-design-prompt.md`, and `commission-breakdown-design/wireframe.html`. Published the exact site bundle under `dist/` in the private GitHub repository `https://github.com/krishmjradiusagent/commission-breakdown` and deployed GPT Site version 2 at `https://radius-office-commissions.radiusagent-2682.chatgpt.site`.
- Verification: Parsed both inline scripts, ran `git diff --check`, and visually tested the local 390px mobile layout. Confirmed the plus control placement, three-option selector, pre-split editor, post-split editor, shared Credit/Referral sheet with Credit selected by default, and removal of the stray `$3,210` from the collapsed header. Confirmed the GPT Site deployment succeeded and the private GitHub repository contains the final revision at commit `bcf3d4e3e78c46546f2312baee3cf83f8c3d99a8`.
- Unresolved: Production iOS and Android integration still requires the native repository paths, payload mapping, permission wiring, and native parity validation. Light-theme, cross-role, and physical-device checks remain advisable. A fully offline package may need the external Phosphor icon dependency bundled locally.
- Next action: Hand the private repository and GPT Site URL to the mobile developers, then use the design-to-native workflow against the actual iOS and Android repositories and validate each supported role against the prototype.

### 2026-09-20 — Integrated commission wallet and final-net reconciliation

- Goal: Consolidate the mobile commission summary into a coherent banking-card-style component, improve the detailed-breakdown entry, restore all required fee and commission information, and ensure the displayed Net commission represents the agent's final take-home.
- Decisions: Keep Sale price and Gross commission as a compact two-card financial header. Place `Your Breakdown`, the persistent Net commission result, and Confirm/Comment inside one rounded commission-wallet shell without adding extra summary cards. Use restrained premium neutral, champagne, bronze, olive, and gold treatments instead of bright childlike category colors; card icons inherit their own label color. Keep caps as one full-width card per item. Show Plan as static `80/20 Standard` until Edit is active, and move assigned/not-charging plan context behind the Plan information treatment. Keep question-mark actions beside commission labels, not dollar amounts. Present Commission basis, Plan fees, Post-split deductions, Payable to Radius, and Commissions as separate count-and-total disclosures. Replace the centered purple `View detailed breakdown` CTA with a quiet `Commission breakdown` / `View all` utility row. Source every agent-facing Net commission from the final per-agent calculation chain after plan fees, post-split deductions, and payable-to-Radius items. Remove the synthetic `Pre-split deductions` subtotal that duplicated the other fee groups and made the visible waterfall appear to subtract `$6,559` twice.
- Files changed: Updated `Commission breakdown.html`; appended this session record to `Sessions.md`; added durable hierarchy, calculation, and verification notes to `Notes.md`.
- Verification: Repeatedly reviewed the local dark Agent view at the mobile viewport in the in-app browser. Confirmed the integrated wallet, premium financial cards, full-width cap cards, compact disclosure groups, Net commission card, Confirm/Comment actions, and the new `View all` row. Verified `View all` opens the full-page Detailed breakdown and that `$32,985` is consistent across the compact result, Mark Perez participant row, detailed commission totals, and final agent chain. Reconciled the current example as `$49,430 × 80% − $3,715 − $2,349 − $495 = $32,985`. Parsed both inline scripts, confirmed 266 unique IDs, and ran `git diff --check` successfully.
- Unresolved: Production commission values still require payload wiring and role-by-role reconciliation. The editable side `AWARD` values are currently presentation state and do not participate in `compute()`; product must clarify whether a fixed award increases gross commission or reallocates the existing gross between sides before the prototype math changes. The final integrated wallet and revised premium palette have not received a complete light-theme and cross-role visual pass.
- Next action: Confirm the business meaning of percentage-plus-flat side awards, connect the final-net chain to production payload fields, and run light-theme plus Agent-in-group, Team Lead, Group Lead, and Group Lead-as-agent regression checks.

### 2026-09-21 — Post-cap plan controls and breakdown overflow menu

- Goal: Add the missing reached-cap preview, make post-cap plans visible and changeable in the correct detailed context, and expand the Commission Breakdown three-dot menu.
- Decisions: Add `In progress` and `Reached` to Design tweaks. Keep post-cap plans only inside Detailed breakdown under Mark Perez's Plan section; do not show a banner on the summary page or at the top of Detailed breakdown. Present Group and Team post-cap plans as separate full-width selector cards with no nested banner padding. Each selector opens a searchable in-place dropdown, recalculates the breakdown when changed, and offers `Add post-cap plan`; custom plans support flat or percentage charges. Expand the three-dot menu with Detailed breakdown, Wire instructions, CDA, Commission settings, and Commission tracker; Commission tracker targets the Home commissions section because no standalone tracker page exists. The user owns manual visual verification for the remainder of the session unless they explicitly ask to test.
- Files changed: Updated `Commission breakdown.html`, `Implemented Home.html`, `dist/Commission breakdown.html`, and `dist/Implemented Home.html`.
- Verification: No final browser or automated verification was performed after the user explicitly requested manual verification only. Source and `dist` were kept synchronized. Deployment status is recorded by the publishing result for this session.
- Unresolved: The latest direct-selector and custom-plan treatment still needs the user's manual visual approval. Post-cap plans and cap progress remain prototype data until production payload mapping is available. Commission tracker remains a Home-section destination rather than a standalone page.
- Next action: Map cap state and available post-cap plans from production data, then perform only the user-requested validation pass.
